# Code Self-Assessor Report: `AttendanceUtil.js`

**File:** [AttendanceUtil.js](file:///E:/NAST/Dazzling/GAS/DazzlingDB/DBServices/AttendanceUtil.js) (5,678 bytes)
**Environment:** Google Apps Script (GAS) — Common Utility Layer

---

## 📊 Robustness Score: 8 / 10 — **Strong**

> `AttendanceUtil.js` is a robust utility module providing safe date/time parsing, conversion, and overnight duration calculations. It is highly compliant with cross-realm Date checking (RULE-10) using `globalThis.isDate`. Hardening parsing error types and removing string-replacement date-parsing workarounds will elevate this to production-grade.

---

## ✅ Step 1 — Context Resolution

| Axis | Assessment |
|---|---|
| Language | Google Apps Script (ES6+) |
| Scope | Common Utility Layer — Handles date/time transformations |
| Realm Safety | Compliant with cross-realm library scopes |
| Overnight Support | Safe rollover calculation logic |

---

## 🔴 Critical Issues

> No critical issues found in `AttendanceUtil.js`.

---

## 🟠 High Priority Issues

> No high priority issues found in `AttendanceUtil.js`.

---

## 🟡 Medium & 🟢 Low Priority Issues

### MED-01 · Locale-Dependent String Replace for Date Parsing

- **Cause:** `convertJsonToDate` utilizes a string replacement logic `cleanDateStr.replace(/-/g, '/')` to normalize ISO dash format dates for browsers like Safari.
- **Scenario:** Running date parses on diverse servers or machines with customized locale settings can result in incorrect month-day interpretations.
- **Impact:** Potential parsing failures or shifts in timezone interpretation.
- **Fix:** Parse components explicitly and instantiate using direct numerical arguments:
  ```javascript
  const parts = cleanDateStr.split(/[-/]/);
  return new Date(parts[0], parts[1] - 1, parts[2]);
  ```

### MED-02 · Generic Exceptions on Parse Failures

- **Cause:** Lines 20, 23, and 26 throw generic `Error` instances for invalid input formatting.
- **Impact:** Violates RULE-06. Boundary interceptors cannot cleanly distinguish these input errors from generic system bugs.
- **Fix:** Raise `SheetDB.ValidationError` instead of generic errors.

### LOW-01 · Realm-Safe Date Fallback still uses `instanceof Date`
- **Location:** `convertDateToJson` fallback path.
- **Problem:** Falls back to `value instanceof Date` when `globalThis.isDate` is unavailable.
- **Fix:** Standardize fallback using `Object.prototype.toString.call(value) === '[object Date]'`.

---

## 💪 Strengths

- **Realm Safety Integration:** Avoids typical GAS cross-realm date comparison failures.
- **Rollover Computation:** Correctly calculates elapsed duration even when schedules cross midnight.

---

## 🚀 Strategic Recommendations

1. **Adopt Standardized Error Classes:** Use validation exception classes to represent formatting issues.
2. **Explicit Date Instantiation:** Avoid browser-workaround string manipulations for date parsing.

---
*Generated by Gemini CLI `code-self-assessor` Skill*
